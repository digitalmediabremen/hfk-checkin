
const unit = 8;

const theme = {

    spacing: (u: number) => u * unit,
    borderRadius: unit * .5,
    primaryColor: "#D81830",
    secondaryColor: "#FFF",
    textColor: "#D81830",
    disabledColor: "#666"
}

export default theme;