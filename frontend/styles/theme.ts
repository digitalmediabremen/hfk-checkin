
const unit = 8;

const theme = {

    spacing: (u: number) => u * unit,
    borderRadius: unit * 0.5,
    primaryColor: "#D81830",
    secondaryColor: "#FFF",
    textColor: "#D81830",
    disabledColor: "gray"
}

export default theme;